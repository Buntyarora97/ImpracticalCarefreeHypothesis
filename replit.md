# Project Notes

Converted the PHP e-commerce storefront in `important/` from Livvra branding to GLIMLACH branding. The admin panel under `important/admin/` was intentionally left unchanged.

The preview workflow serves the PHP site from `important/` on port 5000. `important/mysql_import.sql` contains the ready-to-import GLIMLACH MySQL seed data and product catalog.
